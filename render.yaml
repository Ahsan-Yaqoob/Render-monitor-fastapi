services:
  - type: web
    name: render-monitor-fastapi
    runtime: python
    pythonVersion: "3.12"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: RENDER_API_KEY
        sync: false
      # NOTE: do NOT use the name RENDER_SERVICE_ID — Render reserves it and auto-injects
      # this service's own id, shadowing your value. Use MONITOR_SERVICE_ID instead.
      - key: MONITOR_SERVICE_ID
        sync: false
      - key: AI_BACKEND_URL
        sync: false
      - key: EMAIL_SENDER
        sync: false
      - key: EMAIL_PASSWORD
        sync: false
      - key: EMAIL_RECEIVER
        sync: false
      - key: FASTAPI_PORT
        value: 8000
      - key: DEBUG
        value: false
